%div
  %h1.font-bold.text-xl Tickets
  %table.table-auto
    %thead
      %th Id
      %th Typ lístku
      %th Datum a čas skenování
    %tbody
      - Ticket.all.each do |ticket|
        %tr.odd:bg-white.even:bg-slate-50
          %td= ticket.id
          %td= ticket.ticket_type.name
          %td= ticket.scanned_at
